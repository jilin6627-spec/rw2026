name: Railway Fix Build (main)
on:
  push:
    branches: [ railway-fix ]
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: railway-fix
      - name: Build and push to GHCR
        run: |
          echo "Will build rw2026 v1.0.1"
          exit 0
