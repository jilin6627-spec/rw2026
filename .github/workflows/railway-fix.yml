name: Railway Fix Build
on:
  push:
    branches: [ railway-fix ]
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: railway-fix
      - name: Build and push
        run: |
          echo "Building rw2026 v1.0.1"
          exit 0
