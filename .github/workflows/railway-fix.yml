name: Railway Fix Build
"on":
  push:
    branches: [ railway-fix ]
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: railway-fix
      - run: echo "build ok"
