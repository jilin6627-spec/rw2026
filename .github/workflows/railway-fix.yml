name: Railway Fix Build Minimal
'on':
  push:
    branches:
    - railway-fix
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: echo "Hello from railway-fix build"
    - run: whoami
