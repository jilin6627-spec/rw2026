name: Railway Fix Build
on:
  push:
    branches: [ railway-fix ]
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and push
        run: |
          echo "Building v1.0.1"
          exit 0
